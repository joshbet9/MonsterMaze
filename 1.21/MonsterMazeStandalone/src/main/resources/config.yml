# MonsterMazeStandalone user configuration.
# Map definitions are bundled in maps.yml and are updated with the plugin release.

mode: Original
solo-mode: false
# Emit every completed player's run to solo-runs/ for competition submission.
record-runs: true
map: eyeofender
forced-pattern: -1
mob-overrides:
  volcano: witch
  siberian: creeper

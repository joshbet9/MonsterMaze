"""Entry point for the Monster Maze SOLO Discord bot.

The implementation lives in monster_bot_v2.py so the entry point remains stable
for existing service/startup commands.
"""
from monster_bot_v2 import main

if __name__ == "__main__":
    main()

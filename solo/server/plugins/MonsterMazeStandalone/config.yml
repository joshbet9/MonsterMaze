# MonsterMazeStandalone configuration (solo build)
# Active game mode: Original, Speed, Modern, or Lagless
# Solo players should run Modern (the official board) unless otherwise agreed.
mode: Modern

# Solo mode ON: the plugin uses Solo-specific player conveniences.
solo-mode: true
# Emit every completed Solo run to solo-runs/ for competition submission.
record-runs: true
